package app;

/**
 * Represents a room where the player can interact with a group or student.
 *
 * <p>This abstract class is used as a shared parent for rooms such as the cafeteria and library,
 * where the player completes tasks by talking to NPCs or joining social interactions.
 */
public abstract class JoinGroupHere extends Room {}
